clear; close all; clc;

%%
g = gabor([10 20], [30 330], 'SpatialFrequencyBandwidth', [2, 4]);
figure();
subplot(2, 2, 1);
for p = 1:length(g)
    subplot(2, 2, p)
    imshow(real(g(p).SpatialKernel), []);
end